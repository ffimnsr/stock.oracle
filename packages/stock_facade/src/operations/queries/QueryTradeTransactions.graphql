query QueryTradeTransactions($id: ID!) {
  tradeTransactions(id: $id) {
    id
    stockId
    tradeId
    action
    grossPrice
    shares
    grossAmount
    fees
    netAmount
    transactionDate
    remarks
  }
}