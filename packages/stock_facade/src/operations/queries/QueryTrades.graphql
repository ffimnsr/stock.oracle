query QueryTrades($id: ID!) {
  trades(id: $id) {
    id
    stockId
    transactionDateStart
    transactionDateEnd
    type
    shares
    buyShares
    avgBuyPrice
    sellShares
    avgSellPrice
    status
  }
}